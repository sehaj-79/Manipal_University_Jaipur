<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
package="com.aliferous.mujapp">

<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

<application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:requestLegacyExternalStorage="true"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">
    <activity android:name=".FacultyStatsActivity"></activity>
    <activity android:name=".ResearchActivity" />
    <activity android:name=".FunctionalListActivity" />
    <activity android:name=".ProgramListActivity" />
    <activity android:name=".StatutoryActivity" />
    <activity android:name=".StudentActivity" />
    <activity android:name=".AwardsActivity" />
    <activity android:name=".PlacementActivity" />
    <activity android:name=".FacultyListActivity" />
    <activity android:name=".AboutActivity" />
    <activity android:name=".ChatActivity" />
    <activity android:name=".ClassActivity" />
    <activity android:name=".MainActivity" />
    <activity android:name=".DataActivity" />
    <activity android:name=".RegisterActivity" />
    <activity android:name=".MessageActivity" />
    <activity android:name=".LoginActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
</application>

</manifest>